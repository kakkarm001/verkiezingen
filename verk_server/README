Prequirements: 
- Install playframework (test with activator command in shell)
- Install a database server like wamp. (you probably also want to visualize the database)

Running the project:
run the content of reset_db.sql in your database
To run playframework do "activator run". The default port wil be set on 9000. 
Visit localhost:9000 to complete the compilation. 